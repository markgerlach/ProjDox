using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace mgWinChart.Helpers
{
    public static class GenericEnumConverter
    {
        //public static string StringForValue<T>(T value) where T: struct, IConvertible
        //{
        //    if (!typeof(T).IsEnum)
        //    {
        //        throw new ArgumentException("T must be an enumerated type.");
        //    }
        //    return Enum.GetName(typeof(T), value);
        //}
        //public static T ValueForString<T>(string s) where T: struct, IConvertible
        //{
        //    if (!typeof(T).IsEnum)
        //    {
        //        throw new ArgumentException("T must be an enumerated type.");
        //    }
        //    return (T)Enum.Parse(typeof(T), s);
        //}
    }
}
